# Postgres connection string used by Drizzle (src/db/client.ts) and drizzle-kit.
# Local dev default matches compose.yaml's db service credentials.
DATABASE_URL=postgres://user:pass@localhost:5432/paireval
